{{#each blogposts as |blogpost| }}
<div class="row mb-4 project">
    <div class="col-md-5">
        <h2>
            <a href="/blogpost/{{blogpost.id}}">{{blogpost.title}}</a>
        </h2>

        <p>Created by {{blogpost.user_name}}</p>
    </div>
    <div class="col-md-7">
        <p>
            
        </p>
    </div>
</div>
{{/each}}